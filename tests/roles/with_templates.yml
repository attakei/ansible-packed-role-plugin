comments: |
  It is role-comment. Plugin does not refer to this section.
tasks:
  - debug: msg="here we are in the role, foo={{foo}}"
templates:
  testfile.txt.j2: |
      Hello world. {{ foo }}
      Test
